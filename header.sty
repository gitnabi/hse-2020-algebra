%%% параметры
\setlength{\parskip}{4pt}             % вертикальный отступ после \par
\setlength{\parindent}{0in}           % горизонтальный отступ после \par
\renewcommand{\baselinestretch}{1.1}  % отступ после обычного перевода строки



%%%% Подключаемые пакеты
\usepackage{./packages}  

%%%% Свои команды или сокращения для символов, названий и цветов
\usepackage{./commands}


%%%% Свои окружения и конструкции 
\usepackage{./environments}


%% Номера формул
\mathtoolsset{showonlyrefs=true} % Показывать номера только у тех формул, на которые есть \eqref{} в тексте.
